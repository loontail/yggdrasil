node_modules
dist
.turbo
coverage
*.log
.DS_Store
.env
.env.local

# yggdrasil-client downloads its bundled authlib-injector jar into vendor/
# at build time (`npm run fetch-authlib-injector`). The jar ships via npm's
# `files: ["vendor"]` entry but is not source-controlled.
packages/yggdrasil-client/vendor/*.jar

# VitePress build artefacts
docs-site/.vitepress/cache
docs-site/.vitepress/dist
